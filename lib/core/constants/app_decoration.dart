import 'package:flutter/material.dart';

class AppDecoration {
  static double cardBorderWidth = 2.0;
  static BorderRadius get cardBorderRadius => BorderRadius.circular(10.0);
  static BorderRadius get pokeTypesBorderRadius => BorderRadius.circular(10.0);

  static double filterBorderWidth = 1.0;
  static double filterDividerWidth = 1.0;
  static BorderRadius get filterButtonRadius => BorderRadius.circular(10.0);
  static BorderRadius get filterSelectedItemRadius => BorderRadius.circular(15.0);
}